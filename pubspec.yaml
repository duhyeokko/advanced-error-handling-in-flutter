name: advanced_error_handling
description: An example for advanced error handling
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  import_sorter: 4.4.2
  lint: 1.5.3

flutter:
  uses-material-design: true

import_sorter:
  comments: false